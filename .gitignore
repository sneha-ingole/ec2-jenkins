################################################################################
# Jenkins-specific ignores
# Learn more: https://www.jenkins.io/doc/book/managing/
################################################################################

# Ignore everything in JENKINS_HOME except:
# - jobs directory
# - root-level *.xml configs
# - .gitignore itself
/*
!/jobs
!/*.xml
!/.gitignore

# Allow folder tracking in jobs (git doesn't track empty dirs)
jobs/**
!jobs/**/

# Optional: uncomment to include next build numbers
#!jobs/**/nextBuildNumber

# Ignore builds and indexing (many small files = slow git performance)
builds
indexing

# Don't track workspaces
jobs/**/*workspace

# Only include config.xml files
!config.xml

# Ignore Jenkins secrets (SECURITY RISK if tracked)
#!/secret*

################################################################################
# Terraform-specific ignores
################################################################################

# Terraform state files
*.tfstate
*.tfstate.*
*.tfstate.backup

# Variable definition files (typically local)
*.tfvars
*.tfvars.json

# Terraform logs and caches
.terraform/
terraform/
.terraform.log
.terraform.lock.hcl

# Crash logs
crash.log

################################################################################
# Security-sensitive or generated files
################################################################################

# SSH keys and credentials
*.pem
*.key

# Misc
*.zip
*.log
*.backup
.env

################################################################################
# Editor & OS-specific
################################################################################

# macOS
.DS_Store

# IDEs
.idea/
.vscode/

# Swap/temp files
*.swp
*.sh~
